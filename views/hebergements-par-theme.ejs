<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Hébergements par Thème</title>
  <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <%- include('partials/nav') %>
  <div class="container mt-5">
    <h1 class="text-center">Hébergements pour le thème</h1>
    <div class="row">
      <% housings.forEach(housing => { %>
        <div class="col-md-4">
          <div class="card mb-4">
            <img src="<%= housing.image %>" class="card-img-top" alt="<%= housing.title %>">
            <div class="card-body">
              <h5 class="card-title"><%= housing.title %></h5>
              <p class="card-text"><%= housing.description %></p>
              <p class="card-text">Type: <%= housing.type %></p>
              <p class="card-text">Prix: <%= housing.price %> €</p>
              <p class="card-text">Capacité: <%= housing.capacity %> personnes</p>
            </div>
          </div>
        </div>
      <% }) %>
    </div>
  </div>
  <%- include('partials/footer') %>
  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>


